# TradingView credentials
TV_USERNAME=sufficientHunt88897

# Option A: paste the value of the `sessionid` cookie from browser DevTools
#   (DevTools → Application → Cookies → tradingview.com → sessionid)
TV_SESSION=

# Option B: password login (used by browser automation when TV_SESSION is empty)
TV_PASSWORD=

# Launch browser visible for debugging (default: headless)
TV_HEADLESS=true
